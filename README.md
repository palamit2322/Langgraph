# Langgraph
langgraph hands on

for graph print

pip install grandalf
print(workflow.get_graph().draw_mermaid())
go to mermaid live editor and print these configs adn you will get the graph
print(workflow.get_graph().print_ascii())
